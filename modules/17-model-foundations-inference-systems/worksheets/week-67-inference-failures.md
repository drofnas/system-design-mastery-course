# Week 67 Worksheet: Inference Failures and Measurement

## Evidence identity

- Baseline reference:
- Scenario and shared-input hashes:
- Raw evidence directory and immutability method:
- Warm-up, repetitions, clocks, host, runtime, and limitations:

## Paired trials

For F01–F06 record broken and repaired paths, shared inputs, configuration hashes,
one changed control, target invariant, all repaired invariants, measurements,
alternative causes, and remaining uncertainty.

## Measured CPU profile

Separate queue, tokenization, prefill, first token, ITL, terminal latency, operator
time, memory, profiler overhead, accepted/rejected/failed outcomes, and useful
output. Keep optional accelerator results in a separate table.

## Failure matrix and capacity/cost reconciliation

Reconcile predicted and observed bottlenecks. Recalculate safe admission, degraded
capacity, backlog drain, and cost per quality-passing 1,000 output tokens.

## Learning log

Record the prediction most changed by evidence and why.
