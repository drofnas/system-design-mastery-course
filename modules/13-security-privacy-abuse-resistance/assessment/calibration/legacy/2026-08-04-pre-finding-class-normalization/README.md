# Rejected M13 calibration attempt

The two stored Pass runs predate normalization of the published finding-class tokens. Four non-Pass attempts were rejected before storage. This evidence is preserved for audit only and is not current.
