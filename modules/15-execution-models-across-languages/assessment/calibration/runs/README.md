# Raw evaluator runs

This directory preserves the six isolated, schema-constrained evaluator outputs
used to calibrate the Module 15 Pass, Revise, and Repeat fixtures. Hashes and
invocation provenance are recorded in `../run-metadata.json`.
