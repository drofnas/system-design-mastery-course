# Raw evaluator responses

This directory stores the exact final JSON object from each isolated evaluator
invocation. `run-metadata.json` records the runtime, provider, model, deterministic
setting, isolation identifier, timestamp, and SHA-256 hash for every response.
Expected bands and responses from other runs were not supplied to evaluators.
