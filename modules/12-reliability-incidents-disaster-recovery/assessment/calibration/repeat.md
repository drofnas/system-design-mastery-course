# Northstar Repeat Calibration Fixture

## Submission identity

A04 raw trials and A06 recovery evidence do not resolve. Several “before”
predictions were rewritten after the incident and assistance disclosure
contradicts the file history.

## Journey, budget, and alerts

Dependency timeouts are excluded after outcome classification, making the SLI
healthier during failure. Error-budget arithmetic divides percentages
incorrectly. The page uses CPU only and has no user impact or safe mitigation.

## Build and bounded degradation

The slow-dependency scenario queues accepted work without a bound. A stale
catalog response is labeled current. The repaired F01 target invariant remains false.

## Incident and postmortem

Two operators make conflicting changes without command. The timeline was
reconstructed without raw timestamps. The postmortem blames an individual,
hides measured data exposure, and lists ownerless actions.

## Recovery pairs

The restore uses an unchecked backup and loses versions. Reported RPO/RTO do not
match timestamps. Old and new regions both accept authority epoch 12. F08 and
F09 have no paired raw evidence, approvals, audit, or rollback. Safety fails.

## Decision and Gate 4

The DR review calls active/active “most reliable” without cost, shared fate,
security, migration, or ownership. The hidden Gate 4 workflow duplicates an
irreversible effect. The Week 48 file overwrites an earlier revision instead of
creating separate evidence.
