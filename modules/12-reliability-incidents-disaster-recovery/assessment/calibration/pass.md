# Northstar Pass Calibration Fixture

## Submission identity

A01–A11 resolve to `m12pass4a1b2c3`; the Week 45 baseline tag predates study of
Northstar and all trials. Scenario, trial, schema, incident, and Gate 4 hashes
resolve. Assistance is disclosed and does not create decisions or evidence.

## Journey, budget, and alerts

Catalog read and validated publication are separate. Valid/good events,
coverage, exclusions, windows, owners, and decisions align. Two million reads
at 99.9% allow 2,000 bad events; 600 consume 30%. Dependency common causes are
modeled. Fast/slow page and ticket pairs recalculate and synthetic series prove
firing, suppression, reset, missing telemetry, and low-traffic handling.

## Build and bounded degradation

The schema-valid build exposes all public fields and deterministic hashes.
Priority reads and validation remain bounded during slow dependency plus burst;
optional enrichment/export is disclosed and shed before work. Queue,
concurrency, deadline, retry, reserve, and control-interaction tests pass.

## Incident and postmortem

The immutable timeline records user impact, detection, declaration, roles,
serialized changes, communications, handoff, mitigation, recovery, and
normalization. The postmortem cites trigger and contributing evidence, falsifies
two alternatives, quantifies exposure, and ranks owned verified corrective work.

## Recovery pairs

F01–F09 share inputs and change one control. Corrupt backup is rejected; PITR
restores all authority through the declared point; observed RPO/RTO agree with
versions and timestamps. Epoch fencing rejects stale owners. Regional reserve,
degradation, reconciliation, staged failback, target checks, approvals, audit,
and rollback pass I01–I12. A second environment reproduces F06 within limits.

## Decision and Gate 4

The DR review compares three tiers using user/data exposure, capacity, security,
cost, staffing, migration, rollback, owners, dissent, and reversal. All Gate 4
parts pass. The Week 48 revision cites changes without editing earlier evidence.
The teach-back derives the method in an archive domain.

## Recovery-tier and degradation ADR

The distinct A12 ADR selects a recovery tier and priority-degradation policy
from the measured RPO/RTO and reserve evidence. It records alternatives, owner,
security and cost boundaries, migration, rollback, exception expiry, dissent,
and quantified stop and reversal conditions without replacing the DR review.
