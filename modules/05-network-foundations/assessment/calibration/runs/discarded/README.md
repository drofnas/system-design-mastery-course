# Rejected Evaluator Outputs

These responses are preserved for auditability. Each reported an average that
contradicted its ten detailed rubric scores, so the deterministic checker
rejected it. Neither response contributes to `results.json`.
