# Week 18 Trace Build Worksheet

## Build identity

- Scenario and lab commit:
- Python/OpenSSL/OS versions:
- Loopback and certificate boundary:

## Measured path

Record DNS, TCP, TLS, proxy, application, dependency, and response timings with
raw trial paths and checksums. Distinguish timestamps the application measured
from kernel or network behavior it inferred.

## Trust checks

Show successful hostname validation, rejection under an untrusted anchor, key
cleanup, redaction, and why a test certificate is not a production credential.

## Connection experiment

Compare cold, reused, slow-reader, and exhausted-pool trials. Report active,
peak, wait, completion, cleanup, and useful-work equality.

## Build review

List limitations, unexpected evidence, owners, and changes made after the frozen
Week 17 prediction. Do not edit the prediction.
