# Module 18 raw calibration runs

Store exactly six schema-valid evaluator responses here: Pass, Revise, and Repeat twice. Each run must use a unique isolated read-only session and deterministic settings recorded in `run-metadata.json`. Do not create aggregate readiness results until the calibration checker accepts all six files.
