# Raw evaluator runs

This directory contains six isolated, schema-valid evaluator responses after
calibration execution. Each response is immutable and referenced by
`results.json` and `run-metadata.json` with a SHA-256 digest.
