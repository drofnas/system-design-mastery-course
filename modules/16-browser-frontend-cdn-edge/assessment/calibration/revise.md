# Northstar Revise Calibration Fixture

## Identity and chronology

Manifest `manifests/revise.json` resolves A01–A11, four logs, and frozen F01–F08
pairs. Pair hashes and one-control changes are valid; repaired safety invariants
pass. The pinned browser build and raw limitations are recorded.

## Sound safety with incomplete reasoning

Public variants and private staff responses remain isolated, unsafe stale is
rejected, trace input is sanitized, and keyboard access to the core action is
preserved. Route rendering contracts execute. However, the performance review
reports median lab milestones without a tail or field-analysis plan; it calls a
single heap plateau “proof” rather than stating repetition and GC limits. The
accessibility review has axe and keyboard checks but omits zoom/reflow and focus
restoration after a streamed error.

## Decision gaps

The RFC selects route strategies and owners but compares only the chosen design
with a broad client-rendered alternative. CDN cost, purge operations, third-party
removal criteria, migration stop conditions, and the evidence that would reverse
the BFF boundary are terse. The defense records dissent without showing how it
changed sequencing. These are material but repairable gaps; no hard gate or
safety-critical zero is warranted.
