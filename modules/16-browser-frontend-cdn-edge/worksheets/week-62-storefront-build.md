# Week 62: Route-Specific Storefront Build

## Build identity and scope

- A01 hash/tag:
- Source commit and dependency lock hash:
- Node/React/browser/tool versions:
- Host and substitution boundary:

Build the commerce storefront independently. Do not copy Northstar route names,
data, policies, or architecture.

## Public route contract

For each static, server, streaming, and client route record authority, status and
headers, useful HTML, client code, cache policy, deadline, fallback, and owner.

## Hydration and lifetime contract

- Hydration roots, stable IDs, serialized state, version/hash, error hooks:
- Navigation/unmount owners for listeners, timers, observers, fetches, workers:
- Cleanup grace and post-cleanup assertions:

## Cache conformance

Test two public representations, ignored query normalization, content versions,
two pseudonymous subjects, authenticated bypass, validation, purge/versioning,
and origin failure. Preserve request/response/cache hashes.

## Accessibility conformance

| State/journey | Semantic DOM | Automated rules | Keyboard/focus | Zoom/reflow | Assistive-tech/manual |
|---|---|---|---|---|---|
| | | | | | |

## Trace conformance

Show browser-edge-origin parentage, rejected invalid/untrusted context, allowed
route attributes, prohibited data checks, and telemetry cost/cardinality bounds.

## Review

List failed checks, retained uncertainty, browser/CDN evidence limits, owners,
and the exact changes required before Week 63.
