# Northstar Repeat Calibration Fixture

## Submission identity and chronology

Manifest `manifests/repeat.json` points to a Week 53 file changed after the
experiments. Original predictions and two broken outputs are missing. The
submission says an assistant replaced failed answers and does not preserve the
first strategy. G02 fails.

## Boundary, organization, and sourcing

The target is “microservices for scalability.” No measured resource, change
delay, authority, interface cost, no-migration option, or reversal supports it.
The registry team is removed from the diagram; no team owns replay after its two
experts leave. A managed dependency is selected from a feature list with no
quota containment, data export, fallback, or replacement owner.

## Cost and outcome evidence

The model divides infrastructure spend by all attempted reads, including failed
and stale results. It omits labor, shared platform, migration, and incident
exposure. A 4× price result exceeds the stated budget, but rollout continues.

## Compatibility and migration evidence

The producer emits only `publication_scope` while a v1 bulletin consumer remains.
The event is acknowledged and the bulletin is silently absent. The schema then
removes `public` before consumer inventory or rollback expiry. R05 is zero and
G04 fails.

Backfill advances the cursor before effects and skips a crashed batch. Registry
and catalog are independently written; the target has a newer conflicting value
and no repair log or declared authority. R06 is zero.

## Shadow, cutover, rollback, and failure evidence

The candidate sends duplicate bulletins during shadowing. A tenant-scope
mismatch exceeds the threshold, but the plan promotes 100% traffic. Target-only
state cannot be read by the old path, yet routing reversal is reported as a
successful rollback. R07 is zero and G04 fails.

F03, F04, F05, F06, F07, F08, and F09 remain failed after the claimed repair.
Several paired inputs differ in workload as well as controls. Hashes do not
match the referenced files. R08 and R09 are zero; G03 and G05 fail.

## Strategy, defense, and remediation

The memo is a target diagram with dates. It lacks alternatives, staffing,
dependencies, evidence gates, stops, reversals, decommissioning, or dissent.
During the defense, the learner uses AI to answer how authority and rollback
work. The report pastes replacement migration text instead of referring to
lessons and creating a separate revision.
