# Protocol and Topology Decision

## Status and evidence identity

- Status: Proposed / Accepted / Superseded
- Frozen prediction commit:
- Trial manifest and hashes:
- Decision owner and date:

## User outcome and client population

Define operations, regions/network types, workload, percentiles, availability,
security, cost, and exclusions.

## Path and causal model

Show serial/parallel round trips, bytes, connection reuse, trust boundaries,
proxy/dependency ownership, and failure propagation.

## Alternatives under shared drivers

Compare HTTP/1.1 pooling, HTTP/2, HTTP/3 with fallback, and the current topology.
Separate measured evidence, modeled evidence, standards, and hypotheses.

## Decision

State the chosen protocol/topology, operation scope, assumptions, and why the
evidence supports it.

## Security, privacy, cost, and operations

Cover DNS/certificate/key ownership, capture redaction, connection and memory
capacity, handshake CPU, egress, telemetry, on-call, and dependency contracts.

## Migration, rollback, and reversal

Define compatibility, rollout stages, fallback, stopping thresholds, rollback,
decommissioning, and evidence that reverses the decision.

## Uncertainty and dissent

Record unsupported claims, disagreement, evidence needed, and accountable owner.
